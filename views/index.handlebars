<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title></title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
</head>

<body>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        header {
            width: 100%;
            max-height: 50px;
        }

        .form-container {
            background: #EEE;
            padding: 15px;
        }

        label {
            margin: 0 10px 0 10px;
        }

        .frame {
            width: 100%;
            min-height: 90%;
            background: #ccc;
        }

        .submitButton {
            margin-left: 10px;
        }
    </style>
    <header>
        <div class="form-container">
            <form action="/" method="POST" name="siteName" id="siteName">
                <select name="subject" id="host">
                    <option value="cosmos">Космос</option>
                    <option value="games">Игры</option>
                    <option value="science">Наука</option>
                    <option value="health">Здоровье</option>
                    <option value="politics">Политика</option>
                    <option value="travels">Путешествия</option>
                </select><label for="host">Имя сайта</label>
                <input type="radio" name="mode" value="t" id="all-radio" checked><label for="all-radio">Только заголовок</label>
                <input type="radio" name="mode" value="a" id="news-radio"><label for="news-radio">С аннотацией</label>
                <label for="count">Количество записей на странице</label>
                <select name="count" id="count">
                    <option value="5">5</option>
                    <option value="10">10</option>
                    <option value="all">20</option>
                </select>
                <button type="submit" id="submitButton" class="submitButton">Показать</button>
            </form>
        </div>
    </header>
   <div class="frame">
   <ul class="newslist">
  {{#each titles}}
    <li></li>
  {{/each}}
</ul>

   </div>
   
</body>

</html>