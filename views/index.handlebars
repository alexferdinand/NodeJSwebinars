<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title></title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
</head>

<body>
    <style>
        * {
            margin: 0;
            padding: 0;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        header {
            width: 100%;
            max-height: 50px;
        }

        .form-container {
            background: #EEE;
            padding: 15px;
        }

        label {
            margin: 0 10px 0 10px;
        }

        .frame {
            width: 100%;
        }

        .submitButton {
            margin-left: 10px;
        }

        .newslist {
            margin: 30px;
        }

        .newslist li {
            margin-bottom: 10px;
        }

        .newslist h1 {
            font-size: 16px;
        }

        .pubDate {
            font-size: smaller;
            color: #999;
        }


    </style>
    <header>
        <div class="form-container">
            <form action="/" method="POST" name="siteName" id="siteName">
                <select name="subject" id="host">
                    <option value="cosmos" {{#if cookie.cosmos}} selected {{/if}}>Космос</option>
                    <option value="games" {{#if cookie.games}} selected {{/if}}>Игры</option>
                    <option value="science" {{#if cookie.science}} selected {{/if}}>Наука</option>
                    <option value="health" {{#if cookie.health}} selected {{/if}}>Здоровье</option>
                    <option value="politics" {{#if cookie.politics}} selected {{/if}}>Политика</option>
                    <option value="travels" {{#if cookie.travels}} selected {{/if}}>Путешествия</option>
                </select><label for="host" {{#if cookie.host}} selected {{/if}}>Тема</label>
                <input type="radio" name="mode" value="t" id="all-radio" {{#if cookie.t}} checked {{/if}}><label for="all-radio">Только
                    заголовок</label>
                <input type="radio" name="mode" value="a" id="news-radio" {{#if cookie.a}} checked {{/if}}><label for="news-radio">С аннотацией</label>
                <label for="count">Количество записей на странице</label>
                <select name="count" id="count">
                    <option value="5" {{#if cookie.five}} selected {{/if}}>5</option>
                    <option value="10" {{#if cookie.ten}} selected {{/if}}>10</option>
                </select>
                <button type="submit" id="submitButton" class="submitButton">Показать</button>
            </form>
        </div>
    </header>
    <div class="frame">
        <ol class="newslist">
            {{#each news}}
            <li>
                <h1>
                    {{title}}
                </h1> <span class="pubDate">{{pubDate}}</span>
                {{#if description}}
                <p class="description">{{description}}</p>
                {{/if}}
            </li>
            {{/each}}
        </ol>

    </div>

</body>

</html>